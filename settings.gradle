rootProject.name = "dashloader-core"

enableFeaturePreview "VERSION_CATALOGS"

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from files("libs.versions.toml")
        }
    }
}